import { split } from "./split";

//split이라는 함수는 문자열을 찾아와서 구분자를 기준으로 문자열을 재정리 후 "배열"로 반환하는 함수!
console.log(split("hello"), split("h_e_l_l_o", "_"));

//배열/튜플
//자료구조에 대한 개념
//(*같은 결과를 만들어내는데 선언형 방식(*highlevel) vs 명령형 방식 = 함수형 방식(*lowlevel))

